# Uptime Monitor

Lightweight uptime monitor with Docker and CI.

## Project structure
- app/      - application code
- config/   - targets config (YAML)
- tests/    - tests
- Dockerfile, compose.yml - container setup
